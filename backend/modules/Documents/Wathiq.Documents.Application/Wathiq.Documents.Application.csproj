<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="..\..\..\common.props" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <RootNamespace>Wathiq.Documents</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Wathiq.Documents.Domain\Wathiq.Documents.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Volo.Abp.Ddd.Application" Version="10.3.0" />
    <!-- Abstractions only: the module ENQUEUES jobs; which engine runs them (Hangfire, in the
         host) is a composition-root decision, exactly like IFileStore vs LocalDiskFileStore. -->
    <PackageReference Include="Volo.Abp.BackgroundJobs.Abstractions" Version="10.3.0" />
  </ItemGroup>

</Project>
