namespace Wathiq.Ai;

public static class AiDbProperties
{
    public const string DbSchema = "ai";                 // database.md DB1
    public const string ConnectionStringName = "Ai";     // falls back to "Default" (see DocumentsDbProperties)
}
